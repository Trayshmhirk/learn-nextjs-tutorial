// add _ before folder names to make the folder private in app router
// use %5F to add an actual underscore to the url folder.. this is theh url-encoded form of underscore

export default function Lib() {
   return <div>You cannot view this in the browser</div>;
}
